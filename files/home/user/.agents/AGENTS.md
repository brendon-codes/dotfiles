- Never put emoji in code
- Always use `ctx7` instead of `npx ctx7`.
- Always provide a `--scope` argument to `vercel`. Example `vercel --scope foo`.
- Always provide a `--profile` argument to `aws`. Example `aws --profile foo`.

