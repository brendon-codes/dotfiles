prefix_rule(pattern=[[
  "jq",
  "pgrep",
  "kill",
  "docker",
  "agent-browser",
  "ctx7",
  "find",
  "fd",
  "fdfind",
  "rg",
  "grep",
  "ps",
  "sed"
]], decision="prompt")

prefix_rule(pattern=[
  "bun",
  "run"
], decision="prompt")

prefix_rule(pattern=[
  "bun",
  "test"
], decision="prompt")

prefix_rule(pattern=[
  "bunx"
], decision="prompt")

prefix_rule(pattern=[
  "npm",
  "run"
], decision="prompt")

prefix_rule(pattern=[
  "npm",
  "test"
], decision="prompt")

prefix_rule(pattern=[
  "npx"
], decision="prompt")

prefix_rule(pattern=[
  "pnpm",
  "run"
], decision="prompt")

prefix_rule(pattern=[
  "pnpm",
  "test"
], decision="prompt")

prefix_rule(pattern=[
  "pnpx"
], decision="prompt")

prefix_rule(pattern=[
  "uv",
  "run"
], decision="prompt")

prefix_rule(pattern=[
  "git",
  "add"
], decision="prompt")

prefix_rule(pattern=[
  "git",
  "commit"
], decision="prompt")

prefix_rule(pattern=[
  "git",
  "branch"
], decision="prompt")

prefix_rule(pattern=[
  "gh",
  "pr"
], decision="prompt")

prefix_rule(pattern=[
  "aws"
], decision="prompt")

prefix_rule(pattern=[
  "vercel"
], decision="prompt")

prefix_rule(pattern=[
  "wrangler"
], decision="prompt")

