prefix_rule(pattern=[[
  "npm",
  "pnpm",
  "uv",
  "jq",
  "bun",
  "pgrep",
  "kill",
  "docker",
  "agent-browser",
  "ctx7",
  "find",
  "fd",
  "fdfind",
  "rg",
  "grep",
  "ps",
  "sed"
]], decision="ask")

prefix_rule(pattern=[
  "git",
  "add"
], decision="ask")

prefix_rule(pattern=[
  "git",
  "commit"
], decision="ask")

prefix_rule(pattern=[
  "git",
  "branch"
], decision="ask")

prefix_rule(pattern=[
  "git"
], decision="ask")

prefix_rule(pattern=[
  "gh",
  "pr"
], decision="ask")

prefix_rule(pattern=[
  "gh"
], decision="ask")

prefix_rule(pattern=[
  "aws"
], decision="ask")

prefix_rule(pattern=[
  "vercel"
], decision="ask")

prefix_rule(pattern=[
  "wrangler"
], decision="ask")

